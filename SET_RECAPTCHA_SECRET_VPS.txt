🔐 SET RECAPTCHA SECRET KEY DI VPS BACKEND
================================================

LANGKAH-LANGKAH:

1. SSH ke VPS:
   ssh root@43.228.213.128

2. Edit docker-compose.yml untuk tambah environment variable:
   cd /root/n8n-production/portfolio
   nano docker-compose.yml

3. Tambahkan RECAPTCHA_SECRET_KEY di bagian backend environment:
   
   backend:
     environment:
       - NODE_ENV=production
       - PORT=5000
       - DB_HOST=db
       - DB_PORT=5432
       - DB_NAME=portfolio_cms_2026
       - DB_USER=portfolio_user
       - DB_PASSWORD=SuperStrongPassword123!
       - JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
       - RECAPTCHA_SECRET_KEY=6Lem8T8sAAAAAKoXBEUc3u7gZ5i5zFNEm1hYGuQq

4. Restart backend container:
   docker-compose restart backend

5. Cek logs untuk memastikan tidak ada error:
   docker-compose logs -f backend

6. Test reCAPTCHA di production:
   - Buka https://4leafclover.id
   - Scroll ke Contact section
   - Isi form dan centang "I'm not a robot"
   - Klik Send Message
   - Harus berhasil kirim

CATATAN:
- Secret Key sudah ada fallback di code (recaptchaVerify.js)
- Tapi lebih baik set via environment variable untuk security
- Jangan commit Secret Key ke Git!

STATUS SAAT INI:
✅ Frontend: Site Key sudah di porto/.env.production
✅ Backend: Secret Key ada fallback di code
⏳ VPS: Perlu set RECAPTCHA_SECRET_KEY di docker-compose.yml (optional tapi recommended)
