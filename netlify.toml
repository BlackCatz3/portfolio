[build]
  base = "porto"
  command = "npm run build"
  publish = "dist"

[build.environment]
  VITE_API_BASE_URL = "https://api.4leafclover.id/api"

# IMPORTANT: Make sure Netlify environment variable is also set to:
# VITE_API_BASE_URL = https://api.4leafclover.id/api
# NOT http://43.228.213.128/api

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
